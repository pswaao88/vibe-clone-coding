# 로컬 개발 환경 설정 가이드 (무료)

Blaze 플랜 없이 로컬에서 완전히 테스트할 수 있는 방법입니다.

## 🚀 빠른 시작

### 1단계: Firebase 에뮬레이터 시작

```bash
# Firestore 에뮬레이터만 시작 (가장 가벼움)
npm run emulators:firestore

# 또는 Functions + Firestore 모두 시작
npm run emulators:all
```

에뮬레이터가 시작되면:
- **Firestore**: http://localhost:8080
- **Functions**: http://localhost:5001 (선택사항)
- **에뮬레이터 UI**: http://localhost:4000

### 2단계: 더미 데이터 주입

새 터미널에서:

```bash
npm run seed:emulator
```

이 명령어는 Firestore 에뮬레이터에 더미 데이터를 주입합니다:
- 사용자 3명 (판매자2, 구매자1)
- 상품 7개 (다양한 상태와 카테고리)

### 3단계: React 앱 실행

또 다른 터미널에서:

```bash
npm start
```

React 앱이 http://localhost:3000 에서 실행됩니다.

## 📋 전체 워크플로우

### 터미널 1: Firestore 에뮬레이터
```bash
npm run emulators:firestore
```

### 터미널 2: 더미 데이터 주입 (한 번만)
```bash
npm run seed:emulator
```

### 터미널 3: React 앱
```bash
npm start
```

## 🔧 환경 변수 설정

`.env` 파일을 생성하고 다음 내용을 추가:

```bash
# Firebase 설정 (에뮬레이터 사용)
REACT_APP_FIREBASE_API_KEY=your-api-key
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
REACT_APP_FIREBASE_APP_ID=your-app-id

# 로컬 개발 시 에뮬레이터 사용
REACT_APP_USE_EMULATOR=true
```

## 🎯 테스트 방법

### 1. 상품 목록 확인
- http://localhost:3000/market 접속
- 더미 상품들이 표시되는지 확인

### 2. 상품 검색 테스트
- 검색창에 "노트북" 입력
- 필터 버튼 클릭

### 3. 내 상품 관리
- 로그인 후 `/my-products` 접속
- CSS가 제대로 적용되는지 확인

### 4. 상품 등록
- "상품 등록" 버튼 클릭
- 새 상품 등록 테스트

## 🐛 문제 해결

### 에뮬레이터가 시작되지 않는 경우

1. **포트 충돌 확인**
   ```bash
   # Windows
   netstat -ano | findstr :8080
   ```

2. **Java 설치 확인**
   - Firestore 에뮬레이터는 Java가 필요합니다
   - Java 11 이상 설치 필요

### 더미 데이터가 주입되지 않는 경우

1. **에뮬레이터 실행 확인**
   - http://localhost:4000 접속하여 확인
   - Firestore가 실행 중인지 확인

2. **환경 변수 확인**
   ```bash
   # Windows PowerShell
   $env:FIRESTORE_EMULATOR_HOST="localhost:8080"
   ```

3. **수동으로 데이터 확인**
   - 에뮬레이터 UI (http://localhost:4000)에서
   - Firestore 탭 클릭
   - 데이터가 있는지 확인

### React 앱이 에뮬레이터에 연결되지 않는 경우

1. **Firebase 설정 확인**
   - `src/shared/utils/firebase.js` 확인
   - 에뮬레이터 모드가 활성화되어 있는지 확인

2. **환경 변수 확인**
   - `.env` 파일이 프로젝트 루트에 있는지 확인
   - `REACT_APP_USE_EMULATOR=true` 설정 확인

## 📝 더미 데이터

### 사용자
- **seller@example.com** (판매자1) - 50,000원
- **buyer@example.com** (구매자1) - 30,000원
- **seller2@example.com** (판매자2) - 75,000원

### 상품
- 중고 노트북 (500,000원) - 판매중
- 아이패드 프로 (800,000원) - 판매중
- 무선 이어폰 (200,000원) - 판매중
- 게이밍 키보드 (150,000원) - 예약중
- 모니터 27인치 (300,000원) - 판매완료
- 나이키 운동화 (80,000원) - 판매중
- 책상 (50,000원) - 판매중

## 💡 팁

1. **에뮬레이터 재시작 시**
   - 더미 데이터가 사라지므로 다시 주입해야 합니다
   - `npm run seed:emulator` 실행

2. **데이터 초기화**
   - 에뮬레이터를 중지하고 다시 시작하면 데이터가 초기화됩니다
   - 필요시 `npm run seed:emulator` 다시 실행

3. **에뮬레이터 UI 활용**
   - http://localhost:4000 에서 데이터를 직접 확인/수정 가능
   - 실시간으로 데이터 변경 사항 확인

## 🎨 CSS 스타일

모든 페이지의 CSS가 추가되었습니다:
- ✅ 상품 관리 페이지
- ✅ 상품 업로드 페이지
- ✅ 검색 및 필터
- ✅ 채팅 페이지
- ✅ 프로필 페이지
- ✅ 포인트 충전 폼

## ✅ 체크리스트

로컬 개발 환경이 제대로 설정되었는지 확인:

- [ ] Firebase 에뮬레이터 실행 중
- [ ] 더미 데이터 주입 완료
- [ ] React 앱 실행 중
- [ ] http://localhost:3000 접속 가능
- [ ] 상품 목록이 표시됨
- [ ] CSS 스타일이 제대로 적용됨
- [ ] 검색 및 필터 작동
- [ ] 상품 등록 가능

이제 Blaze 플랜 없이도 완전히 로컬에서 테스트할 수 있습니다! 🎉

